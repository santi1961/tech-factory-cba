let perfilComprador = prompt("Le recordamos que solo hacemos ventas mayoristas esta de acuerdo? (S/N)");

if (perfilComprador = "S") {
    alert = ("Puede proceder a comprar");
} else {
    alert = ("Lo sentimos, solo realizamos ventas mayoristas");
}

